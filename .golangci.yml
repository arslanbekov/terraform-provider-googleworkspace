version: "2"

linters-settings:
  errcheck:
    exclude-functions:
      - github.com/hashicorp/terraform-plugin-sdk/v2/helper/schema.(*ResourceData).Set
      - fmt.Fprintf
      - fmt.Fprintln

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
